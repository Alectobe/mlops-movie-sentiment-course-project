# 📘 Model Card — IMDB Sentiment Analysis (TF-IDF + Logistic Regression)

## 1. Краткое описание модели

Модель предназначена для анализа тональности отзывов на английском языке по датасету IMDB.  
Она классифицирует текст как:

- **positive** — положительный отзыв  
- **negative** — отрицательный отзыв  

Модель построена на классической pipeline-архитектуре:

- TF-IDF векторизация текста  
- Logistic Regression в качестве классификатора  

Проект создан в рамках курсовой работы по дисциплине  
**«Операционализация моделей машинного обучения»**,  
и демонстрирует полный MLOps-контур: эксперименты (ClearML), обучение, артефакты, FastAPI-инференс и Docker-контейнеризацию.

---

## 2. Архитектура и компоненты

### 🔹 Предобработка (TF-IDF)
- токенизация встроенной логикой `scikit-learn`
- нижний регистр
- удаление английских стоп-слов
- N-граммы: `(1,1)` или `(1,2)` в зависимости от версии эксперимента
- ограничение словаря: `5000` или `10000` признаков

### 🔹 Модель (Logistic Regression)
- `max_iter=1000`  
- `n_jobs=-1`  
- регуляризация L2  
- solver по умолчанию (`lbfgs`)

Модель линейная, быстрая, хорошо интерпретируется и подходит для baseline-решений по sentiment analysis.

---

## 3. Версии модели (эксперименты)

Все эксперименты зафиксированы в **ClearML**.

| Эксперимент | max_features | ngram_range | preprocessing_version | Accuracy |
|------------|--------------|-------------|------------------------|----------|
| logreg_tfidf_v1 | 5000 | (1,2) | tfidf_v1 | ~0.88 |
| logreg_tfidf_v2_max10000 | 10000 | (1,2) | tfidf_v2 | ~0.89 |
| logreg_tfidf_unigram | 5000 | (1,1) | unigram_v1 | ~0.86 |

В каждом эксперименте логируются:
- accuracy  
- classification report  
- confusion matrix  
- конфигурация препроцессинга  
- артефакты модели и TF-IDF  

---

## 4. Метрики качества

Основная метрика: **Accuracy**

Диапазон качества на тестовой выборке IMDB:
- **0.87–0.90**

Дополнительные метрики (см. ClearML):
- precision / recall / F1  
- confusion matrix  
- макро-F1  

Модель даёт стабильное качество для простого baseline-решения.

---

## 5. Используемые данные

- Датасет: **IMDB 50k Movie Reviews**
- Количество примеров: 50 000
- Баланс классов: 50% positive / 50% negative
- Формат: CSV (`review`, `sentiment`)
- Источник: Kaggle  

Подробное описание вынесено в `DATASET_CARD.md`.

Train/test split:
- 80% / 20%
- `stratify=y`  

---

## 6. Артефакты модели

Файлы сохраняются в директорию `models/`:

- `logreg_imdb.joblib` — обученный классификатор  
- `tfidf_imdb.joblib` — векторизатор TF-IDF  

Артефакты также загружаются в ClearML и связаны с экспериментами.

---

## 7. Использование модели

### 🔹 Python (локально)

```python
from joblib import load

model = load("models/logreg_imdb.joblib")
vectorizer = load("models/tfidf_imdb.joblib")

text = ["The movie was amazing!"]
vec = vectorizer.transform(text)
prediction = model.predict(vec)

print(prediction)
````

### 🔹 FastAPI-инференс

```
POST /predict
{
  "review": "Amazing movie!"
}
```

Ответ:

```json
{
  "sentiment": "positive",
  "positive_proba": 0.98,
  "negative_proba": 0.02
}
```

---

## 8. Ограничения модели

* Понимает **только английский язык**
* Неустойчива к:

  * сарказму
  * длинным неструктурированным отзывам
  * смешанным тональностям в одном тексте
* Основана на TF-IDF → теряет контекст, не учитывает порядок слов

---

## 9. Возможные улучшения

* Миграция на BERT/DistilBERT/Roberta для учёта контекста
* Предварительная очистка текста (лемматизация, регуляризация)
* Data augmentation
* Перевод сервиса на BentoML или Triton для продакшена
* Добавление мониторинга качества (drift detection)
